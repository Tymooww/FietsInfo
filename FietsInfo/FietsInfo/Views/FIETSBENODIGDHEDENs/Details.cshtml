@model FietsInfo.FIETSBENODIGDHEDEN

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>FIETSBENODIGDHEDEN</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Aspectnaam)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Aspectnaam)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Informatie)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Informatie)
        </dd>

    </dl>
</div>
@if ((bool)new DatabaseModel().ACCOUNT.Find((string)Session["Gebruikersnaam"]).IsAdmin)
{
    <p>
        @Html.ActionLink("Terug", "IndexAdmin")
    </p>
}

@if (!(bool)new DatabaseModel().ACCOUNT.Find((string)Session["Gebruikersnaam"]).IsAdmin)
{
    <p>
        @Html.ActionLink("Terug", "Index")
    </p>
}
